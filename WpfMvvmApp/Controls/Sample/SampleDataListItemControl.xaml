<UserControl x:Class="WpfMvvmApp.Controls.Sample.SampleDataListItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" Background="CadetBlue">
        <Label VerticalContentAlignment="Center" Content="{Binding Path=Text, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <Button>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Content" Value="Delete" />
                    <Setter Property="Command" Value="{Binding Path=Command, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    <Setter Property="CommandParameter" Value="{Binding Path=CommandParameter, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                </Style>
            </Button.Style>
        </Button>
    </StackPanel>
</UserControl>
