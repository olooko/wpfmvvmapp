<Page x:Class="WpfMvvmApp.Views.Sample.ItemsBindingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewModels="clr-namespace:WpfMvvmApp.ViewModels.Sample"
      x:Name="Page"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Page.DataContext>
        <viewModels:ItemsBindingPageViewModel />
    </Page.DataContext>
    <Grid>
        <ItemsControl Margin="50,50,50,50" Background="AliceBlue" ItemsSource="{Binding SampleList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <Label VerticalContentAlignment="Center" Content="{Binding Text}"/>
                        <Button Content="Delete" Command="{Binding ElementName=Page, Path=DataContext.DeleteItemCommand}" CommandParameter="{Binding Id}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
