<Page x:Class="WpfMvvmApp.Views.ListBoxPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewModels="clr-namespace:WpfMvvmApp.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Page.DataContext>
        <viewModels:ListBoxPageViewModel />
    </Page.DataContext>

    <Grid>
        <ListBox HorizontalAlignment="Center" VerticalAlignment="Center">
            <ListBoxItem>
                <Border BorderBrush="{DynamicResource ColorBrandForeground2Brush}" Width="50" Height="50" />
            </ListBoxItem>
            <ListBoxItem>
                <Border BorderBrush="{DynamicResource ColorBrandForeground2Brush}" Width="50" Height="50" />
            </ListBoxItem>
            <ListBoxItem>
                <Border BorderBrush="{DynamicResource ColorBrandForeground2Brush}" Width="50" Height="50" />
            </ListBoxItem>
            <ListBoxItem>
                <Border BorderBrush="{DynamicResource ColorBrandForeground2Brush}" Width="50" Height="50" />
            </ListBoxItem>            
        </ListBox>
    </Grid>
</Page>
